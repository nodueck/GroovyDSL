@startUml
<% domainModel.domainObjects.each { out << generator.domainObjectToPlantUml(it)	} %>
<% domainModel.otherDomainAbstractions.each {	out << generator.otherDomainAbstractionsToPlantUml(it) } %>
@enduml 